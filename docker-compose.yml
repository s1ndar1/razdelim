services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: razdelim
      POSTGRES_PASSWORD: razdelim
      POSTGRES_DB: razdelim
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
